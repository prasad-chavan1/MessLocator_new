import { Typography } from "@mui/material";
import React from "react";
import "../index.css";
const Footer = () => {
  return (
    <footer className="footer">
        <Typography variant="h6">MessLocator</Typography>
    </footer>
  );
};

export default Footer;
